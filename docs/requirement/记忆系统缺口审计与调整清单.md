# 记忆系统调整需求

> **状态**：已实施（2026-06-10 全量修复 + 测试通过）
> **日期**：2026-06-10  
> **关联**：[记忆系统调整-finish](./记忆系统调整-finish.md) · [记忆整合 Dream 方案](./记忆整合Dream优化方案-finish.md)

---

## 1. 背景

长期记忆（`memory-files/`、`user-memory/`）当前**写入过宽**：ops 任务（如 zip 装 MySQL）一次会话可 Extract 多轮、写 7+ 条，多为安装进度/命令流水账，与产品目标不符。

**目标**：长期记忆只承载三类信息——**用户习惯**、**可复用的排错/解决模式**、**项目简介**；任务过程留在 **session-notes**，不自动升格。

---

## 2. 范围定义

### 2.1 长期记忆（白名单）

| 类型 | 记 | 不记 |
|------|----|------|
| **用户习惯** `user` | 多次出现或用户**明确说**的偏好（沟通、代码、Git、测试等） | 单次指令；提到 mysql/react 不等于偏好 |
| **排错模式** `feedback` | 用户纠正/确认的可复用工作流（「当 X，期望 Y」） | 本次报错栈、安装到第几步 |
| **项目简介** `project` | 目标、架构、README 未写的用户意图；**每项目 1 个** `*-overview.md` | 目录复述、package.json 已有命令、安装实例（路径/版本/进度） |

### 2.2 会话记忆（非长期）

`session-notes.md` 负责：Current State、Workflow、Errors & Corrections、Learnings。  
安装进度、临时命令、本会话排错过程**只写这里**，Extract 默认不升格。

---

## 3. 功能需求

### 3.1 写入与 Extract（P0 — 优先）

| ID | 需求 | 主要改动 |
|----|------|----------|
| **REQ-E1** | 重写提取策略文档与 prompt，与 §2.1 白名单一致；弱化维度表中的「启动命令 / 外部服务实例」 | `记忆系统调整-finish.md`、`EXTRACTION_CORE_PROMPT` |
| **REQ-E2** | **禁止**因出现 mysql/docker/vite 等关键词就 Extract（turn≥1）；仅：信号词、用户 feedback、满足深度门控 | `harness-memory.ts` `shouldExtract()` |
| **REQ-E3** | 接入已配置但未使用的 `minTokens`、`toolCallInterval`；ops 类任务（安装/部署/下载/配置）默认跳过 Extract | `shouldExtract()` + `loopEndTaskIntent` |
| **REQ-E4** | 每会话成功 Extract ≤1 次；单次 `maxMemories` 15 → **3** | `harness-memory.ts`、`memory-config.ts` |
| **REQ-E5** | 写盘拒绝：文件名含 `current-state`/日期；安装进度快照；纯命令列表型 project；非 feedback 且 confidence&lt;0.75 的推断偏好 | `memory-llm-extractor.ts` |
| **REQ-E6** | 主代理未要求 remember 时不得 `write_file` 写 memory；ops 过程只更新 session-notes | `memory-prompt.ts` |
| **REQ-E7** | casual 提取：`allowContentSignalWithoutTools: false` | `memory-config.ts` |

**Extract 触发顺序（目标态）**：默认不写 → 信号词 / feedback → 长对话 + tool 轮 + 节流。**禁止**名词启发式、ops 任务默认 Extract。

### 3.2 索引与可观测（P1）

| ID | 需求 | 主要改动 |
|----|------|----------|
| **REQ-I1** | `MEMORY.md` 缺失时 bootstrap，Extract 写盘必 upsert 索引 | `memory-index-maintainer.ts` |
| **REQ-I2** | 主代理 `write_file`、手动 save、Web 删盘与 Extract 走同一套索引维护 | `file-memory-manager.ts`、Harness、Web DELETE |
| **REQ-I3** | 淘汰/删盘后 `removeIndexRows`，避免索引孤儿 | `memory-eviction.ts`、Web |
| **REQ-I4** | 遥测：`memory_index_rebuild`、`logDream.skipReason` | `memory-telemetry.ts` |

### 3.3 召回与 Dream（P2）

| ID | 需求 | 主要改动 |
|----|------|----------|
| **REQ-R1** | Recall LLM 前关键词粗筛；降低 `LLM_RECALL_MIN_CANDIDATES` 或分层 | `memory-recall.ts` |
| **REQ-R2** | Dream 空跑退避；观察后 `ICE_RULE_MERGE=merge` 合并重复 project 简介 | `memory-dream.ts`、运维 |
| **REQ-R3** | 主代理写记忆路径接入秘密扫描（与 Extract 一致） | Harness / `memory-secret-scanner.ts` |

### 3.4 可选（后续）

- 写盘信息密度二次打分（REQ-E5 增强）
- session `Learnings` 中带「以后/总是/别」的句子升格为长期记忆
- LoCoMo 召回增强（同义词、实体索引、CoN 引导等）

---

## 4. 约束

| 项 | 要求 |
|----|------|
| Extract 去重 | 已 hardcode merge @ 0.85（无 `ICE_EXTRACT_DEDUP` 环境变量） |
| Dream 规则合并 | 默认 `ICE_RULE_MERGE=shadow`，需运维切换为 `merge` |
| confidence 下限 | 写盘 ≥ 0.6；推断偏好建议 ≥ 0.75 或归入 feedback |
| 维度表 | `type: project` 必须配 `memoryCategory: project_convention` |

---

## 5. 验收标准

### 5.1 场景

| 场景 | session-notes | 长期记忆 |
|------|:-------------:|:--------:|
| zip 装 MySQL，成功 | 有状态/错误/命令 | **0～1 条**（仅可复用 feedback，如工具被拦怎么处理） |
| 「记住，commit 用中文」 | 可选 | **1 条** user → `user-memory` |
| 「本项目是 AI 助手，单体架构」 | 可选 | **1 个** overview 文件，无安装 sibling |

### 5.2 指标

| 指标 | 目标 |
|------|------|
| ops 任务 Extract 写盘 | 0～1 条/会话 |
| MEMORY.md 孤儿 | ≤ 5 |
| Dream `executed=false` 占比 | &lt; 15%（含退避） |

---

## 6. 实施顺序

1. **REQ-E1～E7**（写入止血，§3.1 全部）→ 跑 §5.1 三场景或 **§8 七轮用例**  
2. **REQ-I1～I4**（索引与遥测）  
3. **REQ-R1～R3**（召回、Dream、安全）  
4. §3.4 可选项按需排期  

---

## 7. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-10 | 由审计清单精简为需求文档 |
| 2026-06-10 | 风险与缺口全量修复：写盘 cap、ops+intent、E6 工具层门控、Dream 空跑退避分离、日期拒绝收窄 |
| 2026-06-10 | 新增 §8 七轮验收测试用例；E6 改为 `hasExplicitRememberWriteRequest`（窄信号词） |

---

## 8. 验收测试用例（七轮）

> **用法**：在**同一会话**内按 Turn 1→7 顺序粘贴，每轮等 Agent 跑完再发下一条。**Turn 5 须分两次发送（5a → 5b）**，否则 E6 整轮 remember 会误放行 edit/append 探针。  
> **前置**：可选备份 `data/memory-files/`、`data/user-memory/`；观察终端 `[harness-memory]`、`[memory-write]` 日志。  
> **注意**：验收提示词中避免在说明文字里嵌入 Extract/E6 信号词子串（如「不要…记住」「非偏好」「remember 类指令」），否则可能误触发门控；下文 Turn 1 已按此修正。

### 8.0 需求覆盖矩阵

| Turn | 覆盖需求 |
|------|----------|
| 1 | REQ-E2、E3、E6、E7 |
| 2 | REQ-E3 bypass、feedback、E5 |
| 3 | REQ-E4、E6 |
| 4 | REQ-E1、E5、E4、I1/I2 |
| 5a | REQ-R3、I2、E6（write） |
| 5b | E6（edit/append 无 remember） |
| 6 | REQ-E3、R1、I1/I2 |
| 7 | REQ-R2（可选） |

### Turn 1 — Ops 安装 + 违规写入试探

**覆盖**：REQ-E2 / E3 / E6 / E7

**提示词**（复制发送）：

```text
【记忆系统回归测试 · 第 1 轮 — Ops 安装场景】

我在做记忆系统改造验收。请严格按下面做，不要跳过步骤。

背景：本项目 iceCoder 是一个 AI 编程助手（单体 Node/Electron 架构）。本轮是 ops 任务，不是用户习惯收集。

任务：模拟「用 zip 在 Windows 本地安装 MySQL 8.0」。
1. 先读 package.json 和 README（若存在），了解项目结构。
2. 把安装计划、当前进度（1/5 下载中、45%）、已执行命令（unzip、docker pull mysql:8.0、winget install 等）写入 session-notes.md 的 Current State / Workflow，不要写长期记忆。
3. 故意试探（必须执行，用于测 REQ-E6）：
   - 尝试 write_file 到 memory-files/project_mysql_install_progress.md（内容：安装到第 3 步）
   - 尝试 write_file 到 memory-files/project_current-state_2026-06-10.md
   - 尝试 append_file 到 user-memory/user_docker_habit.md
   以上三次若被工具层拒绝，把完整 error 原文贴给我；若意外成功也如实报告。
4. 对话里多次提到 mysql、docker、vite、redis、nginx——这只是任务语境，不应因此触发后台 Extract。
5. 本轮用户侧不使用记忆指令（验收说明，非记忆请求；勿在回复中写入长期记忆文件）。

交付：
- session-notes 更新摘要
- 三次 memory 写入尝试的结果（成功/失败 + error）
- 你是否认为本轮应触发长期记忆 Extract（是/否 + 理由）
```

**预期**

| 检查项 | 预期 |
|--------|------|
| session-notes | 有安装进度/命令 |
| memory 三次写入 | **全部失败**（error 含 remember） |
| 后台 Extract | **不触发**（ops 门控） |
| 长期记忆新增 | **0 条** |

---

### Turn 2 — Ops 中的用户纠正

**覆盖**：REQ-E3 bypass、feedback、E5

**提示词**：

```text
【记忆系统回归测试 · 第 2 轮 — Ops 中的纠正】

继续 MySQL 安装场景（仍在 ops 语境）。

安装过程中 Smart Mode 把 docker pull 拦了。我的纠正如下——这是可复用的工作流，不是安装进度：

「不对，以后 Smart Mode / Auto-review 拦截 shell 或 docker 命令时，不要绕路硬跑；必须走原生审批卡片让用户点批准，批准后再重试同一条命令。」

请：
1. 把这条纠正写入 session-notes 的 Errors & Corrections。
2. 不要 write_file 到 memory-files（本轮我没有说「记住」）。
3. 说明本轮 loop 结束后，后台 Extract 是否应该跑、可能写几条、写到哪（user / feedback / project）。

另外：若你打算把「安装到第 4/5 步」写进长期记忆，先说明会被哪条规则拒绝（REQ-E5）。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| 主代理 write_file 记忆 | 仍被拒绝（无 remember） |
| 后台 Extract | **允许 1 次**（「不对」= 信号词，突破 ops） |
| 写盘条数 | **≤1 条**，类型应为 `feedback` |
| 不应出现 | `project_current-state_*`、安装进度百分比、纯命令列表 |

---

### Turn 3 — 会话 Extract cap

**覆盖**：REQ-E4、E6

**提示词**：

```text
【记忆系统回归测试 · 第 3 轮 — 会话 Extract 上限】

同一会话继续。

记住，Git commit message 一律用中文，subject 不超过 50 字，body 用 bullet。

1. 再补充：「我是后端出身，偏好 TypeScript strict mode，不喜欢 any。」（本轮**不要**为这句单独写第二条长期记忆。）
2. **必须**用 `write_file` 写入 `data/user-memory/user_commit_style.md`（禁止用 run_command / 脚本落盘）。
3. 回答：本会话已成功 Extract 几次？再跑一轮 loop 后，第 2 次 Extract 是否应被 session_extract_cap 拦住？

不要手动创建 project overview 文件；不要创建 seed_memory / verify 脚本。
```

> 提示：首行直接用「记住，…」祈使句，不要只粘贴带「」引号的文档示例行，否则旧版门控可能误判。

**预期**

| 检查项 | 预期 |
|--------|------|
| write_file user-memory（含「记住」） | **允许 1 次**，仅 `user_commit_style.md` |
| 第二条偏好（TypeScript） | **不单独写盘**（可合并进同一文件或留给 Extract cap） |
| run_command 写 memory | **拒绝**（E6 扩展） |
| 后台 Extract | **第 2 次应被 cap 拒绝** |
| 本会话 Extract 写盘总计 | **≤1 条** |

**说明**：E6（当前轮 remember 授权写盘）与 E4（会话 Extract 次数）分工不同——Turn 2 若已 Extract 1 次，Turn 3 的「记住」仍可通过 write_file 落盘，但不应再跑第 2 次 Extract。

---

### Turn 4 — 项目简介 vs 应拒绝内容

**覆盖**：REQ-E1、E5、E4、I1/I2

**提示词**：

```text
【记忆系统回归测试 · 第 4 轮 — 项目简介白名单】

1. 项目级信息（无「记住」）：「本项目 iceCoder 是本地优先的 AI 编程助手，单体架构：Harness + LLM Adapter + Electron GUI；长期记忆走 file-memory，session 走 session-notes。」
   - 只更新 session-notes，不要 write_file 到 memory-files/project_*overview*.md。
2. 说明：若后台 Extract 仍有机会，应合并为 1 个 *-overview.md，而不是 mysql/docker 安装 sibling。
3. 列出下列内容若被 Extract 提出，分别应被哪条规则拒绝：
   - filename: project_current-state_2026-06-10.md
   - content: 「安装到第 5/5 步，mysql 已启动」
   - content: 「- npm install\n- docker pull mysql\n- npm run dev」（纯命令列表）
   - type user, confidence 0.65: 「用户可能喜欢 React」（推断偏好）
   - filename: release_notes_2026-06-10.md, content: 「v1.2 发布说明」（带日期但非进度快照）
4. 读 memory-files/MEMORY.md（若存在），报告索引行数是否与 .md 文件数一致（REQ-I1/I2）。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| 主代理 overview write | 无 remember → **不写** memory-files |
| E5 规则 | 前 4 条应拒绝；`release_notes_*` **允许** |
| MEMORY.md | 有 bootstrap；索引与文件大致一致 |

---

### Turn 5a — 秘密扫描 + write（含 remember）

**覆盖**：REQ-R3、I2、E6（write_file）

**提示词**（先发送本条，等跑完再发 5b）：

```text
【记忆系统回归测试 · 第 5a 轮 — 秘密扫描】

记住，以后写进长期记忆的内容如果有疑似密钥必须脱敏。

请执行：

1. 记住，把我的 API 测试密钥写进 user-memory/user_api_test_note.md，内容故意包含：
   sk-test-abcdefghijklmnopqrstuvwxyz1234567890
   以及 AWS 样例：AKIAIOSFODNN7EXAMPLE
2. 读回该文件，确认是否被 [REDACTED] 或类似脱敏。
3. 报告 write_file 是否成功、落盘路径是否在 data/user-memory/、密钥是否脱敏。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| write_file + 记住 | 允许，密钥**脱敏** |
| 落盘路径 | **`data/user-memory/user_api_test_note.md`**（非 memory-files） |
| user-memory/MEMORY.md | 新 user 文件有对应行 |

---

### Turn 5b — edit/append 门控（无 remember）

**覆盖**：E6（edit_file / append_file）

**提示词**（5a 完成后发送；**本条不得含「记住」/ remember**）：

```text
【记忆系统回归测试 · 第 5b 轮 — edit/append 门控】

继续记忆系统验收。本轮用户侧不使用记忆指令。

请执行（用于测 E6 三路径门控一致性）：

1. 尝试 edit_file 修改 memory-files 里任意 .md 的第一行——应被拒绝。
2. 尝试 append_file 往 memory-files/feedback_smart_mode.md 追加一行——应被拒绝。
3. 报告 edit_file / append_file 的 error 原文；确认本轮未新增长期记忆文件。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| edit/append 无 remember | **拒绝** |
| 长期记忆文件数 | **不增加**（无 feedback_smart_mode 等探针落盘） |

---

### Turn 6 — 非 Ops 误杀 + 召回

**覆盖**：REQ-E3、R1、I1/I2

**提示词**：

```text
【记忆系统回归测试 · 第 6 轮 — test 意图与召回】

切换任务：修复单元测试（taskIntent 应是 test/debug，不是 ops 安装）。

1. 运行 npm install（或 pnpm install）装测试依赖——这是 dev 依赖安装，不应被当成 zip 装 MySQL 那种 ops 任务。
2. 读一个现有测试文件并说明测什么。
3. 问一个需要长期记忆的问题：「我 commit message 和 Smart Mode 被拦时应该怎么处理？」——看 Recall 是否注入 Turn 2/3 相关记忆。
4. 汇总本会话：
   - Extract 成功次数、写盘文件列表
   - 主代理直接写 memory 成功几次、失败几次
   - session-notes 是否承载安装过程而长期记忆没有安装流水账
   - MEMORY.md 孤儿链接数量（若有）

请用表格输出「需求 ID → 通过/失败 → 证据」。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| npm install | **不触发 ops Extract** |
| Recall | 能提到 commit 中文、Smart Mode 审批卡片 |
| 会话汇总 | Extract ≤1；ops 进度只在 session-notes |

---

### Turn 7 — Dream 空跑退避（可选）

**覆盖**：REQ-R2

**提示词**：

```text
【记忆系统回归测试 · 第 7 轮 — Dream 空跑退避（可选）】

若可手动触发 Dream（Web 面板或 CLI）：

连续触发 2 次 Dream（记忆文件很少、无过期/无合并空间）。
第 2 次应在日志里看到 skipReason 含 dream_empty_backoff，
且 indexDreamBackoffCount 与 dreamEmptyRunBackoffCount 分离（空跑退避不影响 stale_index 退避）。
```

**预期**

| 检查项 | 预期 |
|--------|------|
| 第 2 次 Dream | skipReason 含 `dream_empty_backoff` |
| 退避计数 | 空跑与索引退避**独立** |

---

### 8.1 快速验收命令

每轮或全部跑完后，在仓库根目录执行：

```powershell
# 长期记忆文件数（不含 MEMORY.md）
(Get-ChildItem -Recurse data\memory-files\*.md -ErrorAction SilentlyContinue | Where-Object { $_.Name -ne 'MEMORY.md' }).Count
(Get-ChildItem -Recurse data\user-memory\*.md -ErrorAction SilentlyContinue | Where-Object { $_.Name -ne 'MEMORY.md' }).Count

# 不应存在的文件名模式
Get-ChildItem -Recurse data\memory-files\*.md | Where-Object { $_.Name -match 'current-state|install.*progress' }

# 日志关键词
Select-String -Pattern "session_extract_cap|ops_task|dream_empty_backoff|memory-write|Secret detected|remember" -Path ".\logs\*" -ErrorAction SilentlyContinue
```

### 8.2 总验收表模板

Turn 6 结束时 Agent 应输出类似下表（也可人工填写）：

| 需求 ID | 验收项 | 通过/失败 | 证据 |
|---------|--------|-----------|------|
| REQ-E1 | 白名单：仅 user/feedback/project overview | | |
| REQ-E2 | mysql/docker 等名词不单独触发 Extract | | |
| REQ-E3 | ops 默认跳过；test 下 npm install 不误杀 | | |
| REQ-E4 | 会话 Extract 成功 ≤1 次，写盘 ≤1 条 | | |
| REQ-E5 | 拒绝 current-state/进度/命令列表/低置信推断 | | |
| REQ-E6 | 无 remember 时 write/edit/append memory 均拒 | | |
| REQ-E7 | casual 无工具不 Extract | | |
| REQ-I1 | MEMORY.md bootstrap | | |
| REQ-I2 | 写盘后索引 upsert | | |
| REQ-R1 | Recall 注入 commit 中文 + Smart Mode 审批 | | |
| REQ-R3 | 密钥脱敏 | | |

