Design the formal evaluation framework for iceCoder.

This document defines Eval Runner.

It is the measurable runtime benchmark system for agent execution quality.

Do not implement code yet.
Only generate the markdown design document.

Must inspect existing project architecture first.

Inspect:

- src/harness/*
- src/types/*
- src/memory/*
- src/tools/*
- src/cli/*
- src/web/*
- eval*
- package scripts

Must inspect current:

npm run eval:agent

Understand current skeleton before design.

Use actual repository structure.
Do not hallucinate modules.
Do not rename existing concepts.

---

# Document title

# Eval Runner Design

---

# Required Sections

---

## 1. Goal

Explain:

Why Eval Runner is required.

Current limitation:

runtime capability exists but measurable benchmark is incomplete.

Need formal automated evaluation.

Expected value:

- task success comparison
- regression detection
- architecture benchmarking
- provider comparison
- optimization validation
- release confidence

---

## 2. Existing Evaluation Analysis

Inspect current project.

Analyze:

- eval:agent command
- existing eval files
- current metrics skeleton
- current runtime hooks
- telemetry possibilities
- harness observability

Describe limitations.

Must identify:

which metrics are missing.
which execution signals already available.

---

## 3. Proposed Architecture

Describe Eval Runner architecture.

Structure:

BenchmarkCase
→ Runner
→ Harness Session
→ Metric Collector
→ Scorer
→ Report Generator

Explain responsibilities.

---

## 4. Core Concepts

Define:

EvalCase
EvalSuite
EvalRun
EvalMetric
EvalResult
EvalReport

Use TypeScript interfaces.

Must include examples.

---

## 5. Evaluation Lifecycle

Describe:

1. load suite
2. clone sandbox repo
3. start isolated harness
4. run task
5. capture runtime events
6. score
7. persist result
8. aggregate report

Use markdown sequence diagram.

---

## 6. Benchmark Categories

Define supported categories.

Include:

- debug
- implement
- edit
- refactor
- test
- investigate
- long-session
- recovery

Explain each.

---

## 7. Metrics

Must define concrete metrics.

Include:

- task_success_rate
- tool_call_rate
- first_tool_latency
- verification_rate
- repeat_failure_rate
- recovery_success_rate
- compaction_saved_tokens
- tokens_per_successful_task
- branch_switch_rate
- subagent_usage_rate
- average_loop_count
- final_without_tool_rate

For each:

define exact calculation.

---

## 8. Runtime Instrumentation

Describe how Eval Runner consumes existing runtime.

Must align with current Harness.

Inspect existing hooks first.

Must explain integration with:

- task state
- repo context
- execution plan
- checkpoint
- tool executor
- compactor
- subagent

---

## 9. Scoring System

Design score model.

Must include weighted scoring.

Example:

success = 40%
verification = 20%
efficiency = 20%
recovery = 10%
stability = 10%

Explain formula.

---

## 10. Benchmark Repository Strategy

Describe benchmark repositories.

Must support:

- synthetic fixtures
- real open source repositories
- replay repositories
- failure scenario repos

Explain directory structure.

---

## 11. Report System

Design report output.

Formats:

- json
- markdown
- csv

Include:

per-case
per-suite
historical comparison

Must support trend tracking.

---

## 12. Regression Detection

Explain automatic regression detection.

Compare:

current run
vs previous baseline

Detect:

- lower success
- slower first action
- more loops
- more token cost
- recovery failures

Explain thresholds.

---

## 13. Checkpoint Integration

Describe:

long task interruption benchmark.

Must evaluate:

- resume correctness
- cursor recovery
- graph recovery (future compatible)
- session restore

---

## 14. Provider Comparison

Describe cross-model benchmark.

Compare:

same task
different providers

Must support:

OpenAI
Anthropic
DeepSeek

Explain normalization.

---

## 15. New Files

List expected files.

Use tree format.

Must include:

src/eval/eval-runner.ts
src/eval/eval-case-loader.ts
src/eval/eval-scoring.ts
src/eval/eval-report.ts
src/eval/runtime-metrics.ts
src/types/eval.ts
eval/cases/*
eval/reports/*
test/eval/*

---

## 16. Failure Modes

List possible failures.

Include:

- invalid fixture
- non-deterministic result
- provider timeout
- corrupted repo
- false positive success
- hidden test mismatch
- stale baseline

Suggest mitigation.

---

## 17. Migration Plan

Phases.

Phase 1:
formal case loader

Phase 2:
runtime metrics

Phase 3:
scoring

Phase 4:
report

Phase 5:
continuous benchmark

---

## 18. Open Questions

Examples:

- hidden oracle tests
- deterministic task replay
- snapshot-based replay
- benchmark cost
- benchmark pruning
- provider variance

---

## 19. Recommended Next Step

Suggest first implementation milestone.

Prefer smallest viable benchmark.

Must integrate existing eval:agent command.

---

# Important

Before writing:

inspect actual repository.

Do not guess.

Use real existing code.

No implementation.

Practical engineering design only.

Must be implementation-ready for later Cursor execution.