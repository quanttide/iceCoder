# iceCoder 运行时后续优化（Phase 5E）

## 状态

**后续规格 · 当前不做。**  
自 [`双模方案2.md`](./双模方案2.md) **V1.3.2** 拆出；主规格当前里程碑止于 **Phase 5D**。5D 验收通过前 **不启动** 本节任一项。

**前置依赖：**

- [`双模方案2.md`](./双模方案2.md) Phase 5D 交付：`EventTimeline`、`RecoverySupervisor` 全链路、`ICE_SUPERVISOR_SHADOW` 可对照
- 现有 `scripts/eval-runner.ts` / `npm run eval:agent` 骨架（可扩展，非阻塞阅读）

---

## 1. 范围与目标

Phase 5E 解决「能跑」之后的 **度量、对照、自学习**：

| 模块 | 目标 |
|------|------|
| **Benchmark 落盘** | 将 Supervisor / Harness 运行指标结构化写入 JSONL/CSV，支持版本间对比 |
| **Eval Runner 扩展** | 真实任务回放、判分、与 shadow 模式联动 |
| **Learning 层** | 基于 `EventTimeline` + eval 结果调参（阈值、触发项），**不**替代 `RecoverySupervisor` 决策链 |
| **验收自动化** | 附录 B 部分检查项脚本化（可选） |

**不在本期（5D）要求：** Learning 闭环、CSV 导出管线、eval 全自动绿。

---

## 2. Phase 5E 任务清单（约 2–3 周）

- [ ] **`Learning` 层**（依赖 `EventTimeline` 与 eval 数据）
  - 输入：`supervisor-events.jsonl`、eval history、shadow「本会接管」记录
  - 输出：对 `supervisor-config.json` 的 **建议补丁**（人工合并）；禁止运行时静默改生产配置
- [ ] **Benchmark 落盘**（本文 §3）
  - 会话级 / 任务级聚合；与 `ICE_SUPERVISOR_SHADOW` 对照导出
- [ ] **`scripts/eval-runner` 扩展**
  - 接入 Supervisor 指标；支持 `adaptive` / `strict` / `off` 矩阵跑批
- [ ] **附录 B 验收自动化**（可选，依赖 eval 管线）
  - 门禁子集优先：`ToolGate` block、`CorrectionPort` 来源 tag、`task_graph_init` 时机
- [ ] **Benchmark CSV / 指标导出**（`switch_frequency`、`recovery_count` 等，见 §3）

---

## 3. Benchmark 指标

> 对应原 [`双模方案2.md`](./双模方案2.md) §22。实现后写入 `data/runtime/supervisor-benchmark.jsonl` 或 eval 输出目录。

### 3.1 必须记录

| 指标键 | 含义 |
|--------|------|
| `success_rate` | 任务是否达到用户目标 / 验证通过 |
| `recovery_count` | takeover 后进入恢复主路径（§10）次数 |
| `switch_frequency` | `free` ↔ `takeover` ↔ `handoff` 切换次数（shadow 含「本会切换」） |
| `wasted_token` | 无效探索、重复失败、被 block 仍消耗的 token 估计 |
| `user_retry` | 用户在同 goal 上重发 / 重开会话次数 |
| `rollback_count` | 经 confirm 的工作区回滚次数 |
| `drift_incidents` | `goal_drift` / `scope_creep` 信号触发次数 |

### 3.2 建议附加字段

| 字段 | 说明 |
|------|------|
| `mode` | `ICE_SUPERVISOR_MODE` |
| `shadow` | 是否 shadow 运行 |
| `domain` | `TaskDomain` |
| `takeover_reason` | 首次 takeover 的 `DeviationSignal[]` 摘要 |
| `recovery_rounds_used` | 相对 `maxRecoveryRounds` |
| `handoff_success` | 是否完成交还 |

### 3.3 导出与对照

- **CSV：** 按任务一行，供 spreadsheet / CI trend
- **Shadow 对照：** 同一输入下 `shadow=true` vs `shadow=false` 的 `switch_frequency` 差分
- **版本标签：** git commit 或 package version 写入每条记录

---

## 4. Learning 层（概要）

**原则：** 离线建议、在线冻结；Learning **不得**在任务中途改写 `supervisorPhase` 或绕过 `ToolGate` / `CorrectionPort`。

```text
EventTimeline + eval history
        ↓
  LearningAnalyzer（批处理 / CLI）
        ↓
  建议：riskThreshold、triggers.*、recovery 预算
        ↓
  人工 review → 合并 supervisor-config.json
```

**可选信号源：**

- shadow 中「本会 takeover 但未接管」→ 阈值是否过松
- `recovery_count` 高但 `success_rate` 低 → 模板图 / 降级路径问题
- `wasted_token` 与 `switch_frequency` 双高 → 稳定窗口 / 冷却参数

**V1 不做的：** 在线 RL、自动 merge 配置、跨租户聚合。

---

## 5. 5D 阶段的替代做法

在 Phase 5E 未实施前，[`双模方案2.md` 附录 B](./双模方案2.md#附录-b验收检查表) 仍有效：

| 5E 能力 | 5D 替代 |
|---------|---------|
| Benchmark CSV | 手工记录或最小 JSONL；`ICE_SUPERVISOR_SHADOW=1` 看 timeline |
| Eval 自动化 | 人工跑附录 B + 代表性任务 |
| Learning | 直接改 `supervisor-config.json`（§15）并 shadow 对照 |

---

## 6. 工期参考（仅 5E）

| 人力 | 日历（专注） |
|------|----------------|
| 1 人 | 约 2–3 周 |
| 与 5D 并行 | **不推荐**；依赖 5D 的 timeline 与 checkpoint 字段稳定 |

---

## 7. 与主规格的关系

| 文档 | 关系 |
|------|------|
| [`双模方案2.md`](./双模方案2.md) | 主实现规格；§21 止于 5D |
| 本文 | 5E 及 benchmark / Learning 的独立 backlog |
| [`双模方案2.md` §23.4](./双模方案2.md#234-预期-benchmark-对比机制分析待-运行时后续优化md-实测) | 竞品对比中的「待实测」指标，由本文 §3 落地后回填 |
