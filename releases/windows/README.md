# Windows 安装包

| 文件 | 说明 |
|------|------|
| [iceCoder-windows.exe](./iceCoder-windows.exe) | NSIS 安装程序，x64，内置 server + Electron |

构建产物位于 `desktop/release/iceCoder-windows.exe`；执行根目录 `npm run build:desktop` 会自动复制到本目录。文件名固定，不含版本号。
